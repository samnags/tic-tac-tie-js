// var prompt = require('prompt');

class Player {
  constructor(token) {
    this.token = token;
  }

  // setToken() {
  //   prompt.start();
  //   prompt.get('piece', (err, result) => {
  //     this.token = result.piece;
  //   });
  // }

  // getMove;
}

module.exports = Player;
